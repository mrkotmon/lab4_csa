# Схемы процессора

Актуальные схемы `DataPath` и `Control Unit` приведены в корневом [`README.md`](../README.md) в формате Mermaid. Они отражают байтовую память, выровненные 32-битные слова, memory-mapped I/O и однословный входной регистр с `INPUT OVERRUN` вместо скрытой очереди.
